/** Default backend base URL used when a client is created without an explicit `baseUrl`. */
export const DEFAULT_ENDPOINT = 'https://polru.pug.sh'

/** Connect's default per-request timeout for the SDK transports. */
export const DEFAULT_TIMEOUT_MS = 10_000
